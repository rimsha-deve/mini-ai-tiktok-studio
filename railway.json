{
  "deploy": {
    "startCommand": "cd backend && pip install uvicorn fastapi python-multipart && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "restartPolicyType": "ON_FAILURE"
  }
}
